% Não remova estas linhas
\makeatletter

% Codificação de entrada Latin1 (=iso8859-1)
\usepackage[utf8]{inputenc}


% Codificação de saída T1 (padrão do LaTeX para incluir acentos)
\usepackage[T1]{fontenc}

% Para criar automaticamente índices
\usepackage{makeidx}
\usepackage{enumitem}

% Escolha de fontes para o documento
\usepackage{pslatex}
% Seleção específica de fontes. NÃO DESCOMENTE MAIS DE UMA LINHA EM
% CADA SEÇÃO ABAIXO E NÃO DESCOMENTE LINHAS NAS DUAS PRIMEIRAS SEÇÕES
% SE DESCOMENTAR A ÚLTIMA LINHA DESTE BLOCO.
% Os nomes entre parênteses são de fontes similares comuns no Windows.
% Descomente uma das linhas abaixo para mudar a fonte usada para o
% corpo de texto (padrão: Times)
%\renewcommand{\rmdefault}{pnc}% New Century Schoolbook (Textbook)
%\renewcommand{\rmdefault}{bch}% Bitstream Charter
%\renewcommand{\rmdefault}{put}% Utopia
%\renewcommand{\rmdefault}{phv}% Helvetica (Arial)
%\renewcommand{\rmdefault}{ppl}% Palatino (Book Antiqua)
%\renewcommand{\rmdefault}{pbk}% Bookman (Bookman Old Style)
%\renewcommand{\rmdefault}{cmr}% Computer Modern Roman (News 711)
% Descomente uma das linhas abaixo para mudar a fonte usada para os
% títulos (padrão: Helvetica)
%\renewcommand{\sfdefault}{pag}% AvantGarde (Century Gothic)
%\renewcommand{\sfdefault}{cmb}% Computer Modern Bright (Verdana)
%\renewcommand{\sfdefault}{ppl}% Palatino (Book Antiqua)
% Descomente a linha abaixo para usar Helvetica (Arial) tanto para o texto
% quanto para os títulos:
%\renewcommand{\rmdefault}{phv}\renewcommand{\sfdefault}{phv}

% Tamanho das margens e eliminação do rodapé
\usepackage{geometry}
\geometry{nofoot,tmargin=30mm,bmargin=20mm,lmargin=30mm,rmargin=20mm}
% Recuo da primeira linha de cada parágrafo
\setlength{\parindent}{1.25cm}

% Determinar que cada primeiro parágrafo seja também indentado
\usepackage{indentfirst}

% Espaçamento padrão entre linhas, substitua \onehalfspacing por
% \doublespacing para usar espaço duplo
\usepackage{setspace}
\onehalfspacing

% Remover espaço extra no topo da página
\usepackage{calc}
\setlength{\topskip}{0pt}

% Controlar linhas órfãs e viúvas
\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000

% Incluir somente número de página no cabeçalho
\usepackage{fancyhdr}
\fancyhf{}
\rhead{\footnotesize\thepage}
\renewcommand{\headrulewidth}{0pt}

% Definir páginas iniciais de capítulo sem cabeçalho
\fancypagestyle{plain}%
	{\fancyhf{}\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}}

% Criar as três seções básicas da mono (pré-textual, textual e
% pós-textual)
%\newcommand{\frontmatter{\pagestyle{empty}}}
%\newcommand{\mainmatter{\pagestyle{fancy}}}
%\newcommand{\backmatter{\pagestyle{empty}}}

% Numerar as figuras com algarismos arábicos
\renewcommand{\thefigure}{\arabic{figure}}

% Numerar as tabelas com algarismos arábicos
\renewcommand{\thetable}{\arabic{table}}

% Usar formatações tipo AUTOR, 1995 ou (AUTOR, 1995) para citações
\usepackage[authoryear]{natbib}

% Descomente estas linhas a seguir se quiser incluir um glossário
%\usepackage[noprefix,norefpage]{nomencl}
%\makeglossary
%\renewcommand{\nomname}{Glossary}
%\renewcommand{\nomlabel}[1]{\raggedright{\textbf{\textsf{#1~~--}}}}
%\renewcommand{\nomentryend}{.}

% Formatar o ambiente {quotation} (citação) segundo a norma ABNT
\renewcommand{\singlespacing}{\setstretch {\setspace@singlespace}}
\renewenvironment{quotation}%
  {\begin{small}\begin{singlespacing}%
     \begin{list}{}{
	\setlength{\topsep}{0cm}%
	\setlength{\parskip}{0cm}%
	\setlength{\partopsep}{0cm}%
	\setlength{\listparindent}{0.5cm}%
	\setlength{\itemindent}{0.5cm}%
	\setlength{\rightmargin}{0cm}%
	\setlength{\leftmargin}{4cm}%
	\setlength{\parsep}{0pt}}%
    \item\relax}%
  {\end{list}\unskip\end{singlespacing}\end{small}}

% Definir margem esquerda das listas
\setlist{leftmargin=1.8cm,itemsep=0pt,topsep=0pt,parsep=0pt}

% Criar estilos de listas
\newcommand{\alflist}%
	{\renewcommand{\labelenumi}{\alph{enumi})}}
\newcommand{\boldrmlist}%
	{\renewcommand{\labelenumi}{\textbf{\Roman{e numi}.}}}
\newcommand{\Alflist}%
	{\renewcommand{\labelenumi}{\Alph{enumi}.}}
\newcommand{\romlist}%
	{\renewcommand{\labelenumi}{\roman{enumi}.}}
\newcommand{\Romlist}%
	{\renewcommand{\labelenumi}{\Roman{enumi}.}}
\newcommand{\deflist}%
	{\renewcommand{\labelenumi}{\textbf{\arabic{enumi}.}}}
\usepackage{amssymb}
\renewcommand{\labelitemi}{\textbullet}
\renewcommand{\labelitemii}{$\circ$}
\renewcommand{\labelitemiii}{$\ast$}
\renewcommand{\labelitemiv}{--}
% Formatar os capítulos numerados (chapter)
\def\@makechapterhead#1{%
  \vspace*{5cm}%
  {\noindent\LARGE\textbf{\textsf{%
 {\thechapter\hspace{.5cm}}{#1}}}%
    \vspace*{2.5cm}\par%
  }}

% Formatar os capítulos não numerados (addchap)
\def\@makeschapterhead#1{%
  \vspace*{5cm}\par
\begin{center}
  {\noindent\centering\LARGE\textbf{\textsf{%
   {#1}
    }}}
\end{center}
  \vspace*{2.5cm}%
  \par}

% Formatar seções
\def\@makesectionhead#1{%
  \vspace*{2.5cm}%
  {\noindent\large\textbf{\textsf{%
 {\thesection\hspace{.5cm}}{#1}}}%
    \vspace*{2.5cm}\par%
  }}

% Formatar subseções
\def\@makesubsectionhead#1{%
  \vspace*{1.25cm}%
  {\noindent\normal\textbf{\textsf{%
 {\thesubsection\hspace{.5cm}}{#1}}}%
    \vspace*{1.25cm}\par%
  }}

% Formatar ambiente paragraph

\renewcommand\paragraph{\@startsection{paragraph}{4}{\z@}%
  {0ex \@plus1ex \@minus.2ex}%
  {-1em}%
  {\raggedsection\normalfont\size@paragraph\sectfont}}

% Formatar notas de rodapé
\usepackage[hang]{footmisc}
\setlength{\footnotemargin}{.25cm}

% Definir profundidade do índice
\setcounter{tocdepth}{1}

% Formato do índice
 \renewcommand*\l@chapter[2]{
  \ifnum \c@tocdepth >\m@ne
    \addpenalty{-\@highpenalty}
    \setlength\@tempdima{1.5em}
    \begingroup
      \parindent \z@ \rightskip \@pnumwidth
      \parfillskip -\@pnumwidth
      \leavevmode \sectfont
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak\
       \leaders\hbox{$\m@th
        \mkern \@dotsep mu\hbox{.}\mkern \@dotsep
        mu$}\hfil\nobreak\hb@xt@\@pnumwidth{\hss #2}\par
      \penalty\@highpenalty
    \endgroup
  \fi}

%%%% Esta seção define opções para output em PDF %%%%
%% Descomente as linhas abaixo quando for gerarPDF %%
%\usepackage{color}
%\usepackage{hyperref}
%\hypersetup{hypertex,colorlinks=true,linkcolor=blue,filecolor=blue,
% pagecolor=blue, urlcolor=blue}

% Não remova estas linhas
\begin{document}
